version: '2'

services:
  warehouse_web:
    build: ./warehouse
    command: bash -c "python3 ./warehouse/manage.py runserver 0.0.0.0:8001"
    volumes:
      - .:/code
    ports:
      - 8004:8001
    restart: always
      
  nginx:
    build: ./web
    ports:
      - 8099:80
    links:
      - warehouse_web
    depends_on:
      - warehouse_web
